default:
  @just --list

set dotenv-load
set fallback

export RUST_LOG := "debug"

deploy-weth9:
  forge create \
    --private-key $PRIVATE_KEY \
    --rpc-url $JSON_RPC \
    --chain-id $CHAIN_ID \
    src/Weth9.sol:WETH9
