{
  "name": "deploy-uniswap-v3",
  "version": "1.0.0",
  "description": "forge project to deploy uniswap-v3",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd lib/deploy-v3 && yarn install && yarn build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "base64-sol": "^1.1.0"
  }
}
